apiVersion: v1
kind: Service
metadata:
  name: app-service
  labels:
    name: app-service
    app: app
spec:
  type: LoadBalancer
  selector:
    name: appInmueble
    type: netapiInmueble
  ports:
  - port: 80
    targetPort: 5000
    nodePort: 30008
